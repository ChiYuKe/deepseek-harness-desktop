# dsh-review-changes

在 DSH 会话右上角提供“审阅”按钮，查看当前会话工作区中的 Git 变更。

功能：

- 列出已修改、已新增、已删除、已重命名和未跟踪文件；
- 显示每个文件的增删行数；
- 点击文件查看 unified diff；
- 撤销单个文件的本地变更，操作前弹出确认；
- 工作区没有变更时显示空状态。

撤销只针对当前会话工作目录解析到的 Git 仓库，不会执行任意命令。已跟踪文件使用 `git restore`，未跟踪文件只允许删除明确选中的单个文件。
