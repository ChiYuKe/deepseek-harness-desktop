# DSH Background

为 DeepSeek Harness 添加可自定义图片背景。

## 功能

- 在「设置 → 通用设置」中选择本地图片。
- 支持启用/停用、图片强度、暗色遮罩、模糊和铺满/完整显示。
- 图片和全部参数保存在 DSH 的用户设置文件中，重启 Harness 后会自动恢复，不依赖浏览器 localStorage。
- 清除图片后恢复默认背景。

## 使用

安装并启用 `dsh-background` 后，打开设置，在通用设置中找到「背景」即可选择图片。

插件只负责浏览器端展示，不修改 DSH 核心界面源码。
