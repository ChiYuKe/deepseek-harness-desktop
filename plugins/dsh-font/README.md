# dsh-font

DeepSeek Harness 的字体设置插件。

启用后会在“设置 → 通用设置”中提供：

- 界面字体：调整 Harness 的界面字体。
- 输出字体：单独调整助手回复和 Markdown 文本字体。
- 输出字号：调整助手回复的基础字号，留空使用默认值。
- 实时预览和一键恢复默认设置。

字体设置保存在 DSH 的用户设置中，只作用于当前 Web Harness，不会修改系统字体，也不会改变代码块的等宽字体。
